package day30_CustomClass;

public class ShiftToLeft {
    public static void main(String[] args) {


       /*
        /*Sometimes you need to shift the elements left or right. Here is an
example of shifting the elements one position to the left and filling the
last element with the first element:*/

      /*  double[] myList = {1.9, 2.9, 3.4, 3.5};

// Retain/save first element on this temp
        double temp = myList[0];

//shift elements left
        for (int i = 1; i < myList.length; i++) {
            myList[i - 1] = myList[i];
        }

// Move the first element to fill in the last position
        myList[myList.length - 1] = temp;
         */
























    }
}
